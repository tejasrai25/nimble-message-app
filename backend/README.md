# nimble-message-app backend
This project uses [pipenv](https://pipenv.pypa.io/en/latest/) to manage dependencies

### Installation
1. Install pipenv `pip install --user pipenv`
2. Run `pipenv install`
